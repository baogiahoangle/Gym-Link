<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UserGym">
        <activity
            android:name=".WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true" />
        <activity android:name=".BmiActivity" />
        <activity android:name=".WorkoutScheduleActivity" />
        <activity android:name=".AddScheduleActivity" />
        <activity android:name=".WorkoutDetailActivity" />
        <activity android:name=".ExerciseStartActivity" />
        <activity android:name=".NotificationActivity" />
        <activity android:name=".PersonalDataActivity" />
        <activity android:name=".ActivityTrackerActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".PTDetailActivity" />
        <activity android:name=".LogoutActivity" />
    </application>

</manifest>